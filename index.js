// .reduce((args) => {do something}, second param), .filter((args) => {})
// result = function; result <-- then value of variable appears/updates?
// ternary operator (condition) ? {value if true}:{value if false}

// ---- if (function({operation}) !== value) {throw new Error('Error message')} ---
// name file containing test = function.test.js


it('works', ()=> {
    expect(1).toBe(1);
})

// expect(function with test arguments).toBe(expected value)
// scripts in package.json "watch": "jest --watch*.js". // "test" : jest


